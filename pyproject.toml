[build-system]
# Minimum requirements for the build system to execute.
requires = [
    "setuptools >= 0.30",
    "wheel >= 0.30",
    "biotite >= 0.28",
    "numpy >= 1.14, <= 1.21",
    "msgpack >= 0.5.6",
    "cython >= 0.29"
]